<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Scatch | Shop</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-sans bg-gray-100 text-gray-800">

    <!-- Navbar -->
    <nav class="w-full bg-white shadow-md px-6 py-4 flex items-center justify-between fixed top-0 left-0 z-50">
        <!-- Logo -->
        <div class="text-2xl font-bold text-blue-500">
            <a href="/">Scatch</a>
        </div>

        <!-- Navigation Links -->
        <div class="space-x-6 hidden md:flex">
            <a href="/" class="text-gray-700 hover:text-blue-500">Home</a>
            <a href="/shop" class="text-gray-700 hover:text-blue-500">Shop</a>
            <a href="#" class="text-gray-700 hover:text-blue-500">About</a>
            <a href="#" class="text-gray-700 hover:text-blue-500">Contact</a>
        </div>

        <!-- User / Cart Icons -->
        <div class="flex items-center gap-4">
            <a href="#" title="Cart" class="relative">
                <i class="ri-shopping-cart-2-line text-xl hover:text-blue-500"></i>
                <!-- <span class="absolute -top-2 -right-2 text-xs bg-red-500 text-white rounded-full px-1">2</span> -->
            </a>
            <a href="/users/logout" title="Logout">
                <i class="ri-logout-box-r-line text-xl hover:text-red-500"></i>
            </a>
        </div>
    </nav>

    <!-- Page Content Wrapper (push content below fixed navbar) -->
    <div class="pt-20">
